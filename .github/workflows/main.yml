name: CI Check with super linter
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Super-Linter
        uses: github/super-linter@v4.8.4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      
          
